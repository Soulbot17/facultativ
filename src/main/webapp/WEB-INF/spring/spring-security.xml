<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
		http://www.springframework.org/schema/security
		http://www.springframework.org/schema/security/spring-security.xsd">

    <http auto-config="true">
        <intercept-url pattern="/user" access="hasAnyRole('ROLE_USER', 'ROLE_ADMIN')" />

        <form-login
                login-page="/index"
                default-target-url="/user"
                authentication-failure-url="/index?error"
                username-parameter="email"
                password-parameter="password" />
        <logout logout-success-url="/index?logout" delete-cookies="JSESSIONID" />

        <!-- enable csrf protection -->
        <csrf/>
    </http>

    <authentication-manager>
        <authentication-provider user-service-ref="userDetailServiceImplementation"/>
    </authentication-manager>

    <beans:bean id="userDetailServiceImplementation" class="com.epam.webelecty.services.UserDetailServiceImplementation">
        <beans:property name="userDao" ref="userDao"/>
    </beans:bean>

</beans:beans>